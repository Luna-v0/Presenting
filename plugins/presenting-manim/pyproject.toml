[project]
name = "presenting-manim-mcp"
version = "0.1.0"
description = "Repo-local MCP server for Manim presentation workflows"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[tool.uv]
package = false
