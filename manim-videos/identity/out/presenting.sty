% Generated by identity/export_beamer.py from identity/tokens.yaml. Do not edit by hand — change the tokens and re-run.
\ProvidesPackage{presenting}[Presenting identity]
\RequirePackage{xcolor}

% ── palette ────────────────────────────────────────────────────────
\definecolor{PresentingGround}{HTML}{E4DCC6}
\definecolor{PresentingInk}{HTML}{232A24}
\definecolor{PresentingMuted}{HTML}{5F5A4B}
\definecolor{PresentingStructureFill}{HTML}{CFC6AE}
\definecolor{PresentingStructureStroke}{HTML}{8C8470}
\definecolor{PresentingGreenTint}{HTML}{CCE0C6}
\definecolor{PresentingGreenMid}{HTML}{3D6349}
\definecolor{PresentingGreenDark}{HTML}{1E3527}
\definecolor{PresentingYellowTint}{HTML}{F6E5A5}
\definecolor{PresentingYellowMid}{HTML}{A87F20}
\definecolor{PresentingYellowDark}{HTML}{55400E}

% Accent fills carry their own Dark. Ground and structure carry Ink.
% Never light text on a dark fill.
\newcommand{\greenmark}[1]{\colorbox{PresentingGreenTint}{\textcolor{PresentingGreenDark}{#1}}}
\newcommand{\yellowmark}[1]{\colorbox{PresentingYellowTint}{\textcolor{PresentingYellowDark}{#1}}}

% ── beamer assignments ─────────────────────────────────────────────
\setbeamercolor{background canvas}{bg=PresentingGround}
\setbeamercolor{normal text}{fg=PresentingInk}
\setbeamercolor{frametitle}{fg=PresentingInk,bg=}
\setbeamercolor{title}{fg=PresentingInk,bg=}
\setbeamercolor{structure}{fg=PresentingGreenMid}
\setbeamercolor{block title}{fg=PresentingInk,bg=PresentingStructureFill}
\setbeamercolor{block body}{fg=PresentingInk,bg=PresentingGround}
\setbeamercolor{block title example}{fg=PresentingGreenDark,bg=PresentingGreenTint}
\setbeamercolor{block body example}{fg=PresentingInk,bg=PresentingGround}
\setbeamercolor{block title alerted}{fg=PresentingYellowDark,bg=PresentingYellowTint}
\setbeamercolor{block body alerted}{fg=PresentingInk,bg=PresentingGround}
\setbeamercolor{footline}{fg=PresentingMuted}
\setbeamercolor{page number in head/foot}{fg=PresentingMuted}
\setbeamercolor{itemize item}{fg=PresentingGreenMid}
\setbeamercolor{itemize subitem}{fg=PresentingGreenMid}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{frametitle}[default][center]  % title is centered

% ── type ───────────────────────────────────────────────────────────
% requested display/body: Liberation Serif
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage{tgtermes}
\else
  \usepackage{fontspec}
  \setmainfont{Liberation Serif}
  \setsansfont{Liberation Serif}
  \setmonofont{Liberation Mono}
\fi

% Type scale — multipliers on the body step, matching the manim library.
%   display  2.4x
%   title    1.8x
%   claim    1.4x
%   body     1.0x
%   caption  0.8x
%   footer   0.52x
% min legible body: 22.0 (manim font_size units @1080p)

% ── geometry ───────────────────────────────────────────────────────
% safe inset 0.6, gutter 0.5, stroke 3.0, corner radius 0.12 (manim units, 14.222 x 8.0 frame)
\endinput
