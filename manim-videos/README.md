# Manim Videos

Active Manim workspace for this repository.

## Setup

```bash
uv sync
```

## Render A Draft

```bash
uv run manim scenes/title_card.py TitleCard -ql
```

## Notes

- Put new presentation scenes under `scenes/`.
- `main.py` and `rule_to_python.py` are older experiments and may need cleanup before they render on the current Manim version.
